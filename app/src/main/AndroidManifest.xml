<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookiesncrumbs.msalvio.cpe50_ay1718_1stsem_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EnterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FirstActivity" />
        <activity android:name=".SampleListActivity" />
        <activity android:name=".SimpleListActivity" />
        <activity android:name=".BarcodeActivity" />
        <activity android:name=".ProductFinalActivity" />
        <activity android:name=".RelativeLayoutActivity" />
        <activity android:name=".ProductItemActivity" />
        <activity android:name=".SqliteActivity" />
        <activity android:name=".SimpleSqliteActivity"></activity>
    </application>

</manifest>